server:
  port: 8080
  mode: debug  # debug / release

database:
  host: localhost
  port: 5432
  user: neobarter
  password: neobarter123
  dbname: neobarter
  sslmode: disable
  max_open_conns: 100
  max_idle_conns: 10

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "your-jwt-secret-change-in-production"
  expire_hours: 168  # 7 days

sms:
  provider: aliyun
  access_key_id: ""
  access_key_secret: ""
  sign_name: "Easy Barter"
  template_code: ""

oss:
  provider: local  # local（本地存储，开发环境）/ aliyun（阿里云 OSS，生产环境）
  endpoint: ""      # 如 oss-cn-hangzhou.aliyuncs.com
  access_key_id: ""
  access_key_secret: ""
  bucket: "neobarter"

elasticsearch:
  addresses:
    - "http://localhost:9200"

rabbitmq:
  url: "amqp://guest:guest@localhost:5672/"

wallet:
  initial_reward: 100.00  # 注册赠送巴特币数量
